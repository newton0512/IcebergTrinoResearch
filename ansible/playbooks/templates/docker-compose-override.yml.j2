services:
  postgres:
    volumes:
      # PostgreSQL 18+ expects mount at /var/lib/postgresql (image uses versioned subdir inside)
      - {{ data_mount }}/postgres:/var/lib/postgresql
  minio:
    volumes:
      - {{ data_mount }}/minio:/data
  nessie:
    volumes:
      - {{ data_mount }}/nessie:/var/lib/nessie
  trino-fte:
    deploy:
      resources:
        limits:
{% if is_production %}
          memory: 64G
{% else %}
          memory: 16G
{% endif %}
